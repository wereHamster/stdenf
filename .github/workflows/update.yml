name: Update

on:
  workflow_dispatch: {}
  schedule:
    - cron: "22 11 * * *"

permissions:
  contents: read

jobs:
  update:
    name: Update
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: DeterminateSystems/nix-installer-action@ef8a148080ab6020fd15196c2084a2eea5ff2d25 # v22

      - uses: cachix/cachix-action@5f2d7c5294214f71b873db4b969586b980625e71 # v17
        with:
          name: caurea
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com

      - run: nix flake update --commit-lock-file
      - id: nix-flake-changelog
        run: git log -1 --pretty=%B | tail +3 | nix run github:iff/flakers -- --github-output-armor message >> "${GITHUB_OUTPUT}"

      - run: nix develop -c -- scripts/refresh-readme.sh

      - uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          branch: update
          title: Update
          body: "${{ steps.nix-flake-changelog.outputs.message }}"
